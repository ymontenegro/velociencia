/**
 * Curated world-climbs rows. GENERATED — do not hand-edit.
 *
 * Built programmatically from the verified curation JSON merged with the
 * DEM-sampled elevation profiles (see `.work-datasets/`). Numeric facts are
 * universal; localized prose is stored as `{ es, en }`. Re-run the generator to
 * update — never re-type figures here.
 */

import type { ClimbEntry } from "@/lib/datasets/climbs";

export const CLIMBS: ClimbEntry[] = [];
