/**
 * Curated race-calendar rows. GENERATED — do not hand-edit.
 *
 * Built programmatically from the verified curation JSON (see `.work-datasets/`).
 * Factual data (name/date/country/class) only; cite the source per row. Re-run
 * the generator to update.
 */

import type { RaceEntry } from "@/lib/datasets/races";

export const RACES: RaceEntry[] = [];
